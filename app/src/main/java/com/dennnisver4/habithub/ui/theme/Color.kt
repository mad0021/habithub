package com.dennnisver4.habithub.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales - Gradiente Azul a Verde
val DeepBlue = Color(0xFF0A1929) // Azul oscuro profundo
val DarkBlue = Color(0xFF1A237E) // Azul oscuro
val ElectricBlue = Color(0xFF2196F3) // Azul eléctrico
val Cyan = Color(0xFF00BCD4) // Cyan brillante
val Teal = Color(0xFF009688) // Teal
val Green = Color(0xFF4CAF50) // Verde brillante

// Colores claros para modo Light
val LightCyan = Color(0xFFB2EBF2) // Cyan claro
val LightTeal = Color(0xFFB2DFDB) // Teal claro
val LightGreen = Color(0xFFC8E6C9) // Verde claro

// Colores oscuros para modo Dark
val DarkCyan = Color(0xFF006064) // Cyan oscuro
val DarkTeal = Color(0xFF004D40) // Teal oscuro
val DarkGreen = Color(0xFF1B5E20) // Verde oscuro

// Colores de superficie
val SurfaceDark = Color(0xFF121212) // Superficie oscura
val SurfaceLight = Color(0xFFFAFAFA) // Superficie clara

// Colores de acento
val AccentPurple = Color(0xFF9C27B0) // Púrpura de acento
val AccentOrange = Color(0xFFFF9800) // Naranja de acento

// Colores OLED - Negro puro para pantallas OLED
val PureBlack = Color(0xFF000000) // Negro puro OLED
val NearBlack = Color(0xFF0A0A0A) // Casi negro para elevación
val DarkGray = Color(0xFF1C1C1C) // Gris oscuro para contenedores
